if(!require(ggplot2))
{
  print("You are missing the package 'ggplot2', we will now try to install it...")
  install.packages("ggplot2")
  library(ggplot2)
}

if(!require(png))
{
  print("You are missing the package 'png', we will now try to install it...")
  install.packages("png")
  library(png)
}

if(!require(grid))
{
  print("You are missing the package 'grid', we will now try to install it...")
  install.packages("grid")
  library(grid)
}

if(!require(gridExtra))
{
  print("You are missing the package 'gridExtra', we will now try to install it...")
  install.packages("gridExtra")
  library(gridExtra)
}

if(!require(reshape2))
{
  print("You are missing the package 'reshape2', we will now try to install it...")
  install.packages("reshape2")
  library(reshape2)
}

if(!require(plyr))
{
  print("You are missing the package 'plyr', we will now try to install it...")
  install.packages("plyr")
  library(plyr)
}

if(!require(scales))
{
  print("You are missing the package 'scales', we will now try to install it...")
  install.packages("scales")
  library(scales)
}

if(!require(phonTools))
{
  print("You are missing the package 'phonTools', we will now try to install it...")
  install.packages("phonTools")
  library(phonTools)
}

if(!require(Hmisc))
{
  print("You are missing the package 'Hmisc', we will now try to install it...")
  install.packages("Hmisc")
  library(Hmisc)
}